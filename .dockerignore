# Secrets — must never enter the build context / image layers
config.toml
.env

# Not needed in the image
docs/
*.db
*.db-wal
*.db-shm
*.md
.git/
.superpowers/

# Build artifacts — host-specific or regenerated in-image; mirrors .gitignore.
/slskdarr
/internal/observ/web/dist/*
!/internal/observ/web/dist/placeholder.html
/web/node_modules/
